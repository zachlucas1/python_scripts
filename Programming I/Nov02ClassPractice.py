### REVIEW FOR LOOP ###
#is_review_for = False
#is_review_for_us_coins = False


"""is_review_for_us_coins = True
    if (is_review_for_us_coins):
        my_coins = [1, 5, 10, 25, 10, 25, 10, 1, 1]
        sum = 0;
        for coin in my_coins:
            if coin == 1: sum += .01
            if coin == 5: sum += .05
            if coin == 10: sum += .10
            if coin == 25: sum += .25

        print(f'The sum of the coins is {sum}')"""

### Book of Mormon Monetary System ###
is_review_for_bom_coins = True
if (is_review_for_bom_coins:)
    #names
    my_coins = ['onti', 'onti', 'onti', 'antion']
    sum